# docs-governance 使用说明

> 核心定位：把优秀 Agent 的一次性工作，沉淀为项目可继承、可验证、可持续演进的集体能力。
> 它通过治理长期 AI 协作中的项目知识、关键决策和验证证据，让正确做法不随单次会话消失。
> 总入口先路由，再按需启用：活文档、领域上下文与 ADR、变更影响、契约、测试、模块回归和闭环设计。不要一次性铺满目录。

---

## 一、每个组件干什么

### 📘 Skill（方法论 = 说明书，AI 遇到对应场景自动参考）

| Skill | 干什么 | 什么时候触发 |
|---|---|---|
| **docs-governance**（总路由） | 识别用户意图并转到对应专项 Skill；大型变更组织“影响→决策/契约→测试→回归→同步→审计”顺序 | 只说“治理项目/整理文档”，或一次改动跨多个治理面 |
| **living-docs-governance**（活文档治理） | 把项目文档拆成**四件套**；MAP 只做导航。多个长期 Module 的权责、状态归属、单向代码依赖与核心流转进入独立、按需创建的 `ARCHITECTURE.md` | 项目长大、文档跟代码对不上、Module 互相乱调、AI 每次进会话重新摸结构、新人接手找不到北 |
| **product-evolution**（产品文档管理） | 在 docs 下按十阶段管理产品资料，维护需求基线，审查来源与验收证据 | 建立产品管理空间、PRD 版本混乱、需求评审或产品进展审查 |
| **context-and-decisions**（上下文与决策） | `CONTEXT.md` 只管稳定领域语言；`docs/adr/` 一项一文件记录架构、数据库等难回退决策 | 术语反复解释、同词异义、架构/数据库/认证/部署/数据模型选型 |
| **change-impact**（变更影响） | 修改前查代码、数据、接口、测试、文档、部署和回滚；实施后对照实际 diff | 跨模块、高风险、数据库迁移、破坏性接口、问“会影响哪里” |
| **test-collaboration**（测试协作） | 用 `TESTS.md` 盘点现有测试资产，把需求、规则、风险和 Bug 转成 TEST-ID，维护必要/缺失/疑似重复/疑似废弃及执行证据 | 想知道项目有哪些测试、还缺什么测试、修 Bug 后该补哪条测试、交付前核对测试证据 |
| **module-regression**（模块回归） | 大项目模块互相引用，一份 `REGRESSION.md` 台账登记"每个模块的下游 + 可执行验收命令"，改完照单跑回归，防"改 A 悄悄坏 B" | 项目模块多、发生过"改一个坏另一个"、改完不确定牵连面 |
| **contract-first**（契约优先） | 分前后端的项目，用 `CONTRACT.md` 登记**唯一机器契约**，前后端只照它各做各的，防字段漂移（后端把 `userName` 改成 `user_name` 没通知前端 → 集成白屏） | 项目有前后端、接口字段老对不上、前端等后端联调卡住 |
| **loop-design-check**（闭环设计） | 把目标写成可判定条件，选择反馈回路并检查停止条件与人工边界 | 用户明确要设计 loop、持续 agent 或闭环任务 |

### 🤖 Agent（Claude Code 执行适配层）

| Agent | 干什么 |
|---|---|
| **docs-governor** | 活文档的**生成者**：扫项目、按方法论写出/更新四件套文档 |
| **product-docs-manager** | 产品文档管理员：调用 product-evolution 管来源、需求编号、修订与授权，并按需调用变更影响、阶段同步和 PR 审计 |
| **docs-auditor** | 活文档的**审计员**（只读不改）：查四件套有没有漂移、重复、写了不存在的路径、指标是不是编的 |
| **contract-director** | 契约**拥有者**：定契约、改契约、最后逐字段集成对账 |
| **regression-auditor** | 回归**审计员**（只跑只报不修）：照台账跑"本模块+下游"验收命令，退出码终审出红绿摘要 |
| **frontend-dev** | 前端**工人**：只读契约照着做，缺字段就上报，不自己瞎猜接口 |
| **backend-dev** | 后端**工人**：实现必须符合契约，并写"返回必须符合契约"的校验测试 |

Codex / ChatGPT 不依赖这些 Claude agent 文件：当前 agent 直接执行同一 skill；需要并行时可使用宿主提供的 explorer / worker，并在派工提示中明确文件所有权和读写边界。

产品资料可这样交接：“请以产品文档管理员身份，整理这个项目的原始输入、规划、需求池、单需求、版本和验收复盘。先复用已有目录与编号；本次允许修改哪些文件、是否包含状态变更，以我的明确授权为准。”Claude Code 使用 `product-docs-manager`；Codex / ChatGPT 调用 `$product-evolution`。给出项目根、材料位置、管理范围和已有确认，具体规则只在 Skill 维护。

### ⌨️ Command（Claude Code 入口）

| 命令 | 干什么 |
|---|---|
| **/governance-init** | 全新空项目 day-0 最小骨架：精简宪法 + docs/governance.md 检查流程 + 流水账；不预建空目录和可选文档 |
| **/governance** | 对当前项目做完整文档治理，生成/更新四件套 |
| **/governance-audit** | 只读审计——告诉你文档漂移在哪，不动文件 |
| **/governance-sync** | 阶段收尾时用：按"本次改动该同步哪份文档"的矩阵查漏补缺，不只追加日志 |
| **/regression-audit** | 改完跑模块回归（init 参数首跑生成台账草稿）；任何一条红=不可交付 |
| **/contract** | 契约式前后端协作：先定契约，再开发，最后对账 |

### Codex / ChatGPT 入口

| Skill | 典型用法 |
|---|---|
| `$docs-governance` | 总入口：识别该用哪项治理能力并组织顺序 |
| `$living-docs-governance` | 初始化治理、治理当前项目、只读审计、阶段同步、LOG 复盘 |
| `$product-evolution` | 在 docs 下按十阶段建立产品管理文件，或只读审查当前产品需求与交付证据 |
| `$context-and-decisions` | 建立领域词汇表、ADR 索引和单项决策记录 |
| `$change-impact` | 修改前影响分析、迁移/回滚检查、实施后 diff 对照 |
| `$contract-first` | 契约优先的跨端协作与对账 |
| `$test-collaboration` | 测试资产盘点、Bug→TEST-ID、跨端契约证据 |
| `$module-regression` | 按回归台账运行本模块与下游验收 |

---

## 二、关系图

```
Codex 总入口：  $docs-governance ──→ 按意图选择专项 skill

大型变更：      change-impact
                ├─→ context-and-decisions（触发 ADR 时）
                ├─→ contract-first（触发接口变化时）
                └─→ test-collaboration → module-regression
                    → living-docs-governance 同步 → 只读审计

文档治理线：  /governance ──→ docs-governor（写四件套）
              ├─ 多 Module 时按需生成/更新 ARCHITECTURE.md
              /governance-audit ──→ docs-auditor（查漂移）
              /governance-sync ──→ docs-governor（按矩阵收尾同步）
              └─ 方法论来自 living-docs-governance skill

前后端协作线：/contract ──→ contract-director（契约拥有者）
                            ├─ frontend-dev（前端工人）
                            └─ backend-dev（后端工人）
              └─ 方法论来自 contract-first skill

测试协作线：  自然语言触发 ──→ test-collaboration skill ──→ TESTS.md
              （v1 没有专用 agent 和 slash command）

模块回归线：  /regression-audit ──→ regression-auditor
              └─ module-regression skill ──→ REGRESSION.md（引用 TEST-ID）
```

测试协作 v1 直接说：`盘点这个项目的测试资产并生成 TESTS.md`，或 `读取 Bug 清单，把缺少保护的 Bug 登记为 TEST-ID`。先用 skill 跑真实项目试点，再决定是否增加专用 agent 和命令。

任务、负责人、阻塞和项目排期继续放 GitHub Issues、Linear 或项目已有 Tracker；`PROJECT_STATUS.md` 只放当前健康快照。`PROJECT_LOG.md` 超过 200 个事件后可生成 SQLite 本地索引，但数据库不管理 schedule，也不是历史唯一来源。

---

产品管理按“项目初始化 → 市场分析 → 需求调研 → 需求分析 → 原型 → PRD → 需求评审 → 研发 → 测试上线 → 运营反馈”组织；小项目每阶段一份文件，材料多时再拆目录。使用 `templates/product-index.example.md`，复用已有来源；未开展阶段如实标记。例：`$product-evolution 在 docs 下建立十阶段产品管理空间，然后审查`。实际样例见 [本插件产品入口](docs/product/README.md)。

## 三、两种协作模式（契约线）

契约式协作有两种用法，按你的实际场景选：

1. **单会话多 agent**：支持多 agent 的宿主中，由契约拥有者派出前端 + 后端并行开发。Claude Code 可用自定义 agents，Codex 可用内置 worker。适合一个人一个会话内推进。
2. **双终端各自跑**（推荐给习惯双终端的）：终端1 跑前端、终端2 跑后端，**两边各自独立**，没有实时调度——**协调全靠那份 `CONTRACT.md` 文件**（异步媒介）。这时"主任"退化成"契约的拥有者/编辑者"，谁改契约谁就是主任。

不管哪种模式，铁律一致：**接口只在 `CONTRACT.md` 指向的机器契约定义一次，谁都不许在实现里私自偏离；要改接口先改契约。**

---

## 四、专业术语对照（写简历/对外介绍时用）

| 通俗说法 | 学名 |
|---|---|
| 前后端各做各的、只通过 API 沟通的架构 | **前后端分离**（decoupled frontend-backend） |
| 两边写完合到一起调试 | **联调**（joint debugging / integration） |
| 本插件契约线这套方法论 | **契约测试 / 消费者驱动契约（Consumer-Driven Contracts, CDC）** |
| "消费方需求先行"（先看前端要什么再定接口） | 正是 **CDC 的核心原则** |
| 后端写"返回必须符合契约"的测试 | **提供者验证**（provider verification） |
| 契约文档本身 | **API 契约 / 接口规格**（OpenAPI / Swagger） |
| 标杆工具 | **Pact**（契约测试框架） |

> 一句话：本插件契约线 ≈ 一套轻量的 **Consumer-Driven Contract testing** 实践，用机器可读契约 + Markdown 入口 + agent 角色分工落地。

---

## 五、模板与参考

- `templates/` —— 四件套 + ARCHITECTURE + CONTEXT + ADR + CONTRACT + TESTS + REGRESSION 模板；只有触发真实需要时才套
- `references/governance-sync-matrix.md` —— 阶段收尾时判断"本次变化该同步哪份文档"的影响矩阵
- `scripts/audit-cheap.sh [scope] [--format json]` —— 确定性文档审计；范围为 spine/context/adr/artifacts/full，默认文字，JSON 接口见 [结果约定](references/audit-result-format.md)
- `scripts/project-log-index.py` —— LOG 事件计数、经确认归档和 SQLite 重建

---

## 六、安装

Claude Code 插件市场：

```text
/plugin marketplace add Seekers2001/docs-governance
/plugin install docs-governance@docs-governance
```

Codex CLI：

```bash
codex plugin marketplace add Seekers2001/docs-governance
codex plugin add docs-governance@docs-governance
```

安装或升级后新开会话，使新的 skills 与 hooks 被加载。Codex 会要求用户审查并信任插件 hook；未信任 hook 不影响 skills 使用。

## 七、开发验证

首次开发先创建并激活虚拟环境，运行 `python -m pip install -r requirements-dev.txt`，再运行 `bash scripts/verify.sh`。标准入口同时校验 OpenAPI 模板、单元测试和 full 文档审计；插件运行脚本仍只依赖 Python 标准库。审查已提交改动时设置 `DOCS_GOVERNANCE_BASE_REF` 为原始基线提交，CI 会传入 PR 基线或推送前 SHA。

## 八、文件归位与自动审计

按 [规则说明](references/document-policy.md) 配置项目级 `.docs-governance.json`。完整十阶段入口登记为必需文件；按文件名或一级标题关键词声明允许目录及例外。Stop 在内容变化后审计并报告，提交钩子检查暂存区并拒绝违规。创建 PR 前运行 `python3 scripts/check-pr-docs.py --base origin/main`（替换实际插件路径和目标分支）；pre-push 与 PR CI 使用同一入口扫描提交快照，失败阻止继续。`change_rules` 沿用项目已有的模块到文档映射，提示本次需要核对哪些文档。没有每日定时任务，也不自动搬动文件。语义冲突仍需文档审查。
